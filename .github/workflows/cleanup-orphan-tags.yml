name: Cleanup Orphan Tags

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  checks: read

jobs:
  cleanup-orphans:
    uses: octopus-synapse/octopus-workflows/.github/workflows/cleanup-orphan-tags.yml@main
    with:
      bun-version: "1.2.23"
